{
  "name": "entropy-analyzer",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "bin": "./dist/index.js",
  "scripts": {
    "start": "nodemon ./src/index.ts --data=data",
    "compile": "rimraf dist && tsc && caxa '{{caxa}}/node_modules/.bin/node' '{{caxa}}/index.js' --input './dist' --output './entropy-analyzer'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.9",
    "caxa": "^3.0.1",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1"
  }
}
